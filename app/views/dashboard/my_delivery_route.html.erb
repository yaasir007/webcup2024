<%= render 'layouts/top_dashboard', page_title: "Optimised Delivery Route" %>
  <div class="container map-directions">
    <div style="width: 100%; height: 600px; border-radius: 15px;"
      data-controller="delivery-route"
      data-delivery-route-marker-value="<%= @coordinates.to_json %>"
      data-delivery-route-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
  <div class="center-items mt-3">
    <%= link_to 'Back to Orders', my_orders_path, class: "btn btn-warning" %>
    <%= link_to 'Mark as Delivered', mark_as_delivered_path(@order), class: "btn btn-primary" %>
  </div>
<%= render 'layouts/btm_dashboard' %>
